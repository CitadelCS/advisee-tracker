%h2 Student View

-student = student

%table#students.table.table-striped.col-md-12
  %thead
    %tr
      %th Last Name
      %th First Name
      %th Graduation Year
      %th CWID
      %th Academid Advisor
      %th Last Meeting
  %tbody
    %tr
      %td= student.last
      %td= student.first
      %td= student.year
      %td= student.CWID
      %td= student.advisor
      %td= student.lastMeet

= link_to 'Add New Meeting', new_meeting_path

%table#meetings.table.table-striped.col-md-12
  %thead
    %tr
      %th Date
      %th Note Tacker
      %th Notes
  - @meetings.each do |meeting|
    %tbody
      %tr      
        %td= meeting.date
        %td= meeting.noteTacker
        %td= meeting.notes